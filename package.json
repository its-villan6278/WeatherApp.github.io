{
  "name": "weather-app",
  "version": "1.0.0",
  "description": "Api is at /backend FrontEnd is At /frontend",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "cd backend && npm install && tsc && cd dist && node index.js",
    "front": "cd frontend & npm install && react-scripts build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
